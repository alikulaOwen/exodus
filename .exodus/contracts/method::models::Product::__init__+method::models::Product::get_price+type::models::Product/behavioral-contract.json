{
  "schema_version": "1.0.0",
  "contract_id": "contract-method-models-Product-__init___method-models-Product-get_price_type-models-Product",
  "unit_id": "method::models::Product::__init__+method::models::Product::get_price+type::models::Product",
  "unit_name": "Product",
  "unit_kind": "class",
  "source_signature": "Product(name: str, price: int); get_price() -> int",
  "target_signature": "struct Product { name: String, price: i64 }; fn get_price(&mut self) -> i64",
  "assertions": [
    {
      "case_id": "get_price_basic",
      "input": "{ let mut p = Product::new(\"a\".to_string(), 10); p.get_price() }",
      "expected": "10",
      "oracle": "differential_execution",
      "evidence": "differential_execution: python3 scripts/ground_fixture_contracts.py (fixtures/03_module_dependency/{models,service}.py, captured 2026-08-29)"
    },
    {
      "case_id": "serde_roundtrip_is_identity",
      "input": "{ let p = Product::new(\"a\".to_string(), 10); let json1 = serde_json::to_string(&p).unwrap(); let p2: Product = serde_json::from_str(&json1).unwrap(); p2 == p }",
      "expected": "true",
      "oracle": "declared_invariant",
      "evidence": "declared_invariant: serialize-then-deserialize must be a no-op for a derived Serialize/Deserialize/PartialEq struct — this is a structural property, not something differential-executed against the Python source (Python has no equivalent typed round-trip), and is checked without assuming a specific field order (the transform engine's field ordering is not guaranteed stable)."
    }
  ],
  "verification_status": "passed"
}